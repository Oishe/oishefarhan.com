{
  "name": "oishefarhan.com",
  "private": true,
  "type": "module",
  "description": "Wrangler lives here so `npx wrangler deploy` works from the repo root. The build is `just publish` — see WORKFLOW.md. There are deliberately no build scripts in this file; two ways to build is how they drift.",
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
