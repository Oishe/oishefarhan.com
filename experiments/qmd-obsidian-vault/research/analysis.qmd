---
title: Analysis
publish: true
format: html
execute:
  freeze: true
---

# Analysis

This Quarto document links to Markdown: [[note-a|Note A]].

It links to another Quarto document: [[comparison|Comparison]].

## Executable cell

```{python}
#| label: basic-summary
values = [2, 3, 5, 7]
sum(values) / len(values)
```

The source stays `.qmd` so Quarto can bind the Python execution engine.
